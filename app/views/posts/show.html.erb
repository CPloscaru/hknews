<div class="post-details">
  <h2><%= @post.title %></h2>
  <p> <%= @post.url %></p>

  <%= simple_form_for(@comment, method: :post, data) do |f| %>
    <%= f.input :content %>
    <%= f.submit "Leave a comment", input_html: {class: "btn btn-success btn-comment"}%>
  <%= end %>

</div>
